collection:

  # Route
  - pattern: "/github"
    pipeline:

      - type: process
        cmd: ./ci/github.sh

  # Route
  # AWS ECS ping
  - pattern: "/ping"
    pipeline:

      - type: process
        cmd: echo pong
